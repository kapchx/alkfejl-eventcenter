import { LocationErrorsPipe } from './location-errors.pipe';

describe('LocationErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new LocationErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});

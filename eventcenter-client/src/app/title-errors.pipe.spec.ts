import { TitleErrorsPipe } from './title-errors.pipe';

describe('TitleErrorsPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
